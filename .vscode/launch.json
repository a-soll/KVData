{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Main",
            "program": "${workspaceFolder}/debug/test/test",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ],
    "version": "2.0.0"
}
